#spring.profiles.active=dev
#spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
#
##spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
#spring.jpa.properties.hibernate.globally_quoted_identifiers=true
#spring.data.jpa.repositories.enabled=true
#
### Enables or disables multipart file upload support.
#spring.servlet.multipart.enabled=true
#
###  Sets the maximum size for a single file in a multipart request.
#spring.servlet.multipart.max-file-size=5MB
#
### Sets the maximum total size for the entire multipart request
### (including all files + form data).
#spring.servlet.multipart.max-request-size=10MB
#
### false = connection closes immediately after query.
### This aligns with recommendation to avoid lazy-loading
### in views. Default in Spring Boot 3.0+. Always false
### for REST API (never Lazy-loading during JSON serialization)
#spring.jpa.open-in-view=false
#
### Essential for existing db
##spring.flyway.baseline-on-migrate=true
#
## Optional
##spring.flyway.enabled=true
##spring.flyway.locations=classpath:db/migration
##spring.flyway.baseline-version=1
#
## Additional but optional
##spring.flyway.baseline-description=Existing database baseline
#
## JWT configuration
#jwt.secret=your-256-bit-secret-your-256-bit-secret
#jwt.expiration=86400000  # 24 hours in milliseconds



# -----------------------------
# Server
# -----------------------------
server.port=8080

# -----------------------------
# Spring Datasource / MySQL
# -----------------------------
spring.datasource.url=jdbc:mysql://localhost:3306/clockin_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=clockuser
spring.datasource.password=clock123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# -----------------------------
# JWT Configuration
# -----------------------------
jwt.secret=your_super_secret_key_here_which_is_long_enough
jwt.expiration=86400000
# 24 hours in milliseconds

# -----------------------------
# Swagger / OpenAPI
# -----------------------------
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# -----------------------------
# Spring Main
# -----------------------------
spring.main.allow-bean-definition-overriding=true
